# Swiss Generative

This is a code bundle for Swiss Generative. The original project is available at https://www.figma.com/design/9skqPGcNUOFWJaSn14Nfi7/Swiss-Generative.

## Running the code

Run `npm i` to install the dependencies.

Run `npm run dev` to start the development server.